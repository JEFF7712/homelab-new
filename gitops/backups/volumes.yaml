apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-dumps
  namespace: backups
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-cluster
  resources:
    requests:
      storage: 20Gi
