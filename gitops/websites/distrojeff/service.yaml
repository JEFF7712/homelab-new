apiVersion: v1
kind: Service
metadata:
  name: distrojeff-site-svc
  namespace: distrojeff
spec:
  selector:
    app: distrojeff-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
