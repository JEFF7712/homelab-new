apiVersion: v1
kind: Service
metadata:
  name: nixagent-svc
  namespace: nixagent
spec:
  selector:
    app: nixagent-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
