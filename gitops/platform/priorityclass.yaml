apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: homelab-critical
value: 1000000
globalDefault: false
description: >-
  Cluster-critical homelab services (tunnel, home-assistant, monitoring).
  Preempts best-effort workloads under pressure, never evicted first.
