image: nixos/nix:latest

stages:
  - lint
  - test
  - security
  - opnsense

variables:
  NIX_CONFIG: "experimental-features = nix-command flakes"
  TF_STATE_NAME: opnsense-production
  TF_HTTP_ADDRESS: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state/${TF_STATE_NAME}"
  TF_HTTP_LOCK_ADDRESS: "${TF_HTTP_ADDRESS}/lock"
  TF_HTTP_UNLOCK_ADDRESS: "${TF_HTTP_ADDRESS}/lock"
  TF_HTTP_USERNAME: gitlab-ci-token
  TF_HTTP_PASSWORD: $CI_JOB_TOKEN
  TF_HTTP_LOCK_METHOD: POST
  TF_HTTP_UNLOCK_METHOD: DELETE
  TF_HTTP_RETRY_WAIT_MIN: "5"

nix_format:
  stage: lint
  script:
    - cd flake && nix fmt -- --check flake.nix

repository_tests:
  stage: test
  script:
    - nix develop ./flake -c python -m unittest discover -s tests -v
    - nix flake check ./flake

yaml_schema:
  stage: test
  script:
    - nix develop ./flake -c yamllint .

secret_scan:
  stage: security
  script:
    - nix develop ./flake -c gitleaks detect --source . --redact

opnsense_inventory:
  stage: opnsense
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  script:
    - nix develop ./flake -c python -m opnsense_reconciler.inventory --artifact-dir artifacts/opnsense
  artifacts:
    expire_in: 7 days
    paths:
      - artifacts/opnsense/

opnsense_plan:
  stage: opnsense
  variables:
    OPNSENSE_URI: $OPNSENSE_URL
    SSL_CERT_FILE: $OPNSENSE_CA_FILE
    TF_VAR_opnsense_uri: $OPNSENSE_URL
  needs:
    - job: opnsense_inventory
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  script:
    - test "$(nix develop ./flake -c python -c 'import json; print(str(json.load(open("artifacts/opnsense/inventory.json"))["assignment_api_available"]).lower())')" = true
    - nix develop ./flake -c tofu -chdir=tofu/opnsense init -reconfigure
    - nix develop ./flake -c tofu -chdir=tofu/opnsense validate
    - nix develop ./flake -c tofu -chdir=tofu/opnsense plan -refresh-only -out=refresh-only.tfplan
    - nix develop ./flake -c tofu -chdir=tofu/opnsense plan -out=desired.tfplan
  artifacts:
    expire_in: 7 days
    paths:
      - tofu/opnsense/refresh-only.tfplan
      - tofu/opnsense/desired.tfplan

opnsense_apply:
  stage: opnsense
  needs:
    - job: opnsense_inventory
      artifacts: true
    - job: opnsense_plan
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
  script:
    - test "$(nix develop ./flake -c python -c 'import json; print(str(json.load(open("artifacts/opnsense/inventory.json"))["assignment_api_available"]).lower())')" = true
    - nix develop ./flake -c tofu -chdir=tofu/opnsense init -reconfigure
    - nix develop ./flake -c tofu -chdir=tofu/opnsense apply desired.tfplan
    - nix develop ./flake -c python -m opnsense_reconciler.reconcile --assignments opnsense_reconciler/assignments.json --inventory artifacts/opnsense/inventory.json --kea-interfaces opnsense_reconciler/kea-interfaces.json
