title: Spotify
disabled_by: null
domain: spotify
entry_id: 01M2Q0JRQTRWMEXFFTDP8YCHET
pref_disable_new_entities: false
pref_disable_polling: false
source: user
notes: >-
  Source-managed metadata only. The Spotify OAuth token lives in
  `core.config_entries` storage and is not committed. Application credentials
  (client id + secret) are in `application_credentials` storage and not committed;
  they must be migrated to SOPS-backed `!secret` references if used in YAML.
  Reprovision: re-run the Spotify integration config flow in HA with credentials
  stored under `!secret spotify_client_id` / `!secret spotify_client_secret`.
